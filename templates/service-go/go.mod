// Replace the module path with your repo path when you adopt this template,
// e.g. github.com/your-org/your-service. The pipeline reads the Go version here
// (setup-go uses go-version-file: go.mod), so keep it accurate.
module github.com/your-org/service-go

go 1.22
